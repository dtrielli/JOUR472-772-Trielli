knitr::opts_chunk$set(echo = TRUE)
# turn off sci notation
options(scipen=999)
library(tidyverse)
library(lubridate)
library(refinr)
# Load data
md_grant_loans <- read_csv("data/State_of_Maryland_Grant_and_Loan_Data__FY2009_to_FY2022_20250115.csv")
# Display it
md_grant_loans
# Now let's try and group and count the number of expenditures by recipient. To make it a bit more manageable, let's use another string function from `stringr` and filter for recipients that start with the uppercase "W" or lowercase "w" using the function `str_detect()` with a regular expression.
# The filter function in the codeblock below says: look in the Grantee column, and pluck out any value that starts with (the "^" symbol means "starts with") a lowercase "w" OR (the vertical "|", called a pipe, means OR) an uppercase "W".
md_grant_loans |>
group_by(Grantee) |>
summarise(
count=n()
) |>
filter(str_detect(Grantee, '^w|^W')) |>
arrange(Grantee)
cleaned_md_grant_loans <- md_grant_loans |>
mutate(grantee_clean=key_collision_merge(Grantee)) |>
select(Grantee, grantee_clean, everything())
cleaned_md_grant_loans
cleaned_md_grant_loans |>
group_by(Grantee, grantee_clean) |>
summarise(
count=n()
) |>
filter(str_detect(Grantee, '^w|^W')) |>
arrange(Grantee)
cleaned_md_grant_loans <- md_grant_loans |>
mutate(grantee_clean=n_gram_merge(Grantee)) |>
select(Grantee, grantee_clean, everything())
cleaned_md_grant_loans
cleaned_md_grant_loans |>
group_by(Grantee, grantee_clean) |>
summarise(
count=n()
) |>
filter(str_detect(Grantee, '^w|^W')) |>
arrange(Grantee)
# Now let's try and group and count the number of expenditures by recipient. To make it a bit more manageable, let's use another string function from `stringr` and filter for recipients that start with the uppercase "W" or lowercase "w" using the function `str_detect()` with a regular expression.
# The filter function in the codeblock below says: look in the Grantee column, and pluck out any value that starts with (the "^" symbol means "starts with") a lowercase "w" OR (the vertical "|", called a pipe, means OR) an uppercase "W".
md_grant_loans |>
group_by(Grantee) |>
summarise(
count=n()
) |>
filter(str_detect(Grantee, '^w|^W')) |>
arrange(Grantee) |>
print(n = 50)
cleaned_md_grant_loans <- md_grant_loans |>
mutate(grantee_clean=key_collision_merge(Grantee)) |>
select(Grantee, grantee_clean, everything())
cleaned_md_grant_loans |>
print(n = 50)
# Now let's try and group and count the number of expenditures by recipient. To make it a bit more manageable, let's use another string function from `stringr` and filter for recipients that start with the uppercase "W" or lowercase "w" using the function `str_detect()` with a regular expression.
# The filter function in the codeblock below says: look in the Grantee column, and pluck out any value that starts with (the "^" symbol means "starts with") a lowercase "w" OR (the vertical "|", called a pipe, means OR) an uppercase "W".
md_grant_loans |>
group_by(Grantee) |>
summarise(
count=n()
) |>
filter(str_detect(Grantee, '^w|^W')) |>
arrange(Grantee)|>
View()
cleaned_md_grant_loans <- md_grant_loans |>
mutate(grantee_clean=key_collision_merge(Grantee)) |>
select(Grantee, grantee_clean, everything())
cleaned_md_grant_loans|>
View()
cleaned_md_grant_loans |>
group_by(Grantee, grantee_clean) |>
summarise(
count=n()
) |>
filter(str_detect(Grantee, '^w|^W')) |>
arrange(Grantee)|>
View()
cleaned_md_grant_loans <- md_grant_loans |>
mutate(grantee_clean=n_gram_merge(Grantee)) |>
select(Grantee, grantee_clean, everything())
cleaned_md_grant_loans
cleaned_md_grant_loans |>
group_by(Grantee, grantee_clean) |>
summarise(
count=n()
) |>
filter(str_detect(Grantee, '^w|^W')) |>
arrange(Grantee)|>
View()
cleaned_md_grant_loans <- md_grant_loans |>
mutate(grantee_clean=n_gram_merge(Grantee)) |>
select(Grantee, grantee_clean, everything())
cleaned_md_grant_loans
cleaned_md_grant_loans |>
group_by(Grantee, grantee_clean) |>
summarise(
count=n()
) |>
filter(str_detect(Grantee, '^w|^W')) |>
arrange(Grantee) |>
mutate(
comparison = paste0(Grantee, "  →  ", grantee_clean),
changed = Grantee != grantee_clean
) |>
filter(changed == TRUE) |>
select(comparison) |>
head(30)
cleaned_md_grant_loans <- md_grant_loans |>
mutate(grantee_clean=n_gram_merge(Grantee)) |>
select(Grantee, grantee_clean, everything())
cleaned_md_grant_loans
cleaned_md_grant_loans |>
group_by(Grantee, grantee_clean) |>
summarise(count=n()) |>
filter(str_detect(Grantee, '^w|^W')) |>
arrange(Grantee) |>
mutate(
comparison = paste0(Grantee, "  →  ", grantee_clean),
changed = Grantee != grantee_clean
) |>
filter(changed == TRUE) |>
select(comparison) |>
view()
cleaned_md_grant_loans <- md_grant_loans |>
mutate(grantee_clean=n_gram_merge(Grantee)) |>
select(Grantee, grantee_clean, everything())
cleaned_md_grant_loans
cleaned_md_grant_loans |>
group_by(Grantee, grantee_clean) |>
summarise(count=n()) |>
filter(str_detect(Grantee, '^w|^W')) |>
mutate(changed = Grantee != grantee_clean) |>
select(Grantee, grantee_clean, changed) |>
filter(changed == TRUE) |>
head(30)
cleaned_md_grant_loans |>
group_by(Grantee, grantee_clean) |>
summarise(count=n()) |>
filter(str_detect(Grantee, '^w|^W')) |>
mutate(changed = Grantee != grantee_clean) |>
select(Grantee, grantee_clean, changed) |>
filter(changed == TRUE) |>
view()
cleaned_md_grant_loans <- md_grant_loans |>
mutate(grantee_clean=n_gram_merge(Grantee)) |>
select(Grantee, grantee_clean, everything())
cleaned_md_grant_loans
cleaned_md_grant_loans |>
group_by(Grantee, grantee_clean) |>
summarise(count=n()) |>
filter(str_detect(Grantee, '^w|^W')) |>
arrange(Grantee)|>
mutate(changed = Grantee != grantee_clean) |>
select(Grantee, grantee_clean, changed) |>
filter(changed == TRUE) |>
view()
cleaned_md_grant_loans |>
group_by(Grantee, grantee_clean) |>
summarise(
count=n()
) |>
filter(str_detect(Grantee, '^w|^W')) |>
arrange(Grantee)|>
mutate(changed = Grantee != grantee_clean) |>
select(Grantee, grantee_clean, changed) |>
filter(changed == TRUE) |>
view()
comparison_md_grant_loans <- md_grant_loans |>
mutate(
key_collision_clean = key_collision_merge(Grantee),
ngram_clean = n_gram_merge(Grantee)
)
comparison_md_grant_loans |>
filter(str_detect(Grantee, '^w|^W')) |>
group_by(Grantee, key_collision_clean, ngram_clean) |>
summarise(count = n(), .groups = "drop") |>
arrange(Grantee) |>
view()
comparison_md_grant_loans |>
filter(str_detect(Grantee, '^w|^W')) |>
mutate(different = key_collision_clean != ngram_clean) |>
select(Grantee, key_collision_clean, ngram_clean, different) |>
distinct() |>
arrange(Grantee) |>
view()
comparison_md_grant_loans |>
filter(str_detect(Grantee, '^w|^W')) |>
mutate(different = key_collision_clean != ngram_clean) |>
select(Grantee, key_collision_clean, ngram_clean, different) |>
distinct() |>
filter(different = 'TRUE')
comparison_md_grant_loans <- md_grant_loans |>
mutate(
key_collision_clean = key_collision_merge(Grantee),
ngram_clean = n_gram_merge(Grantee)
)
comparison_md_grant_loans |>
filter(str_detect(Grantee, '^w|^W')) |>
mutate(different = key_collision_clean != ngram_clean) |>
select(Grantee, key_collision_clean, ngram_clean, different) |>
distinct() |>
filter(different = 'TRUE')
comparison_md_grant_loans |>
filter(str_detect(Grantee, '^w|^W')) |>
mutate(different = key_collision_clean != ngram_clean) |>
select(Grantee, key_collision_clean, ngram_clean, different) |>
distinct() |>
filter(different == 'TRUE')
comparison_md_grant_loans <- md_grant_loans |>
mutate(
key_collision_clean = key_collision_merge(Grantee),
ngram_clean = n_gram_merge(Grantee)
)
comparison_md_grant_loans |>
filter(str_detect(Grantee, '^w|^W')) |>
mutate(different = key_collision_clean != ngram_clean) |>
select(Grantee, key_collision_clean, ngram_clean, different) |>
distinct() |>
filter(different == 'TRUE')
arrange(Grantee) |>
view()
comparison_md_grant_loans <- md_grant_loans |>
mutate(
key_collision_clean = key_collision_merge(Grantee),
ngram_clean = n_gram_merge(Grantee)
)
comparison_md_grant_loans |>
filter(str_detect(Grantee, '^w|^W')) |>
mutate(different = key_collision_clean != ngram_clean) |>
select(Grantee, key_collision_clean, ngram_clean, different) |>
distinct() |>
filter(different == 'TRUE')
arrange(Grantee) |>
view()
comparison_md_grant_loans <- md_grant_loans |>
mutate(
key_collision_clean = key_collision_merge(Grantee),
ngram_clean = n_gram_merge(Grantee)
)
comparison_md_grant_loans |>
filter(str_detect(Grantee, '^w|^W')) |>
mutate(different = key_collision_clean != ngram_clean) |>
select(Grantee, key_collision_clean, ngram_clean, different) |>
distinct() |>
filter(different == 'TRUE') |>
arrange(Grantee) |>
view()
